# 覆蓋 GitHub 後的操作

1. 解壓縮 ZIP。
2. 進入解壓縮後的 `girlfriend-kuji-v4-formal` 資料夾。
3. 全選資料夾內的檔案，而不是只上傳外層資料夾。
4. 到 GitHub 的 `girlfriend-kuji-v3` repository。
5. 使用 **Add file → Upload files**，把檔案拖進去並允許覆蓋同名檔案。
6. 按 **Commit changes**。
7. 到 Render 查看最新部署，等待顯示 **Live**。
8. 開啟：
   - 玩家：`https://girlfriend-kuji-v3.onrender.com`
   - 管理員：`https://girlfriend-kuji-v3.onrender.com/admin`

建議先建立一個只有少量籤的測試活動，確認選號、扣印章、紀錄與下一彈都正常，再建立正式活動。
